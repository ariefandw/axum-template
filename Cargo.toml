[package]
name = "axum-template"
version = "0.2.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
description = "Production-ready Axum API scaffold: session-backed auth, owned storage, realtime, and an append-only audit log"
readme = "README.md"

[dependencies]
# Web & Async
axum = { version = "0.8", features = ["macros", "multipart"] }
tokio = { version = "1.43", features = ["full"] }
tower = { version = "0.5", features = ["limit", "buffer"] }
tower-http = { version = "0.6", features = ["cors", "trace", "request-id", "timeout", "compression-full"] }
tower_governor = "0.6"

# Serialization & Validation
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
validator = { version = "0.20", features = ["derive"] }

# Database
sqlx = { version = "0.8", features = [
    "runtime-tokio",
    "tls-rustls",
    "postgres",
    "uuid",
    "chrono",
    "migrate",
] }

# Security & Auth
jsonwebtoken = "9.3"
argon2 = { version = "0.5", features = ["std"] }
oauth2 = "5.0"
rand = "0.9"
sha2 = "0.10"
hmac = "0.12"
base64 = "0.22"
chacha20poly1305 = { version = "0.10", features = ["std"] }
subtle = "2.6"
lettre = { version = "0.11", default-features = false, features = ["tokio1-rustls-tls", "smtp-transport", "builder"] }

# Observability & Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# Config & Environment
dotenvy = "0.15"

# OpenAPI & Docs
utoipa = { version = "5.3", features = ["axum_extras", "uuid", "chrono"] }
utoipa-axum = "0.2"
utoipa-scalar = { version = "0.3", features = ["axum"] }

# Types & Utilities
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1.13", features = ["serde", "v4", "v7"] }
thiserror = "2.0"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
governor = "0.8"
metrics = "0.24"
metrics-exporter-prometheus = "0.16"
bytes = "1.10"
regex = "1.11"
tokio-util = { version = "0.7", features = ["io"] }
tokio-stream = { version = "0.1", features = ["sync"] }
futures-util = { version = "0.3", default-features = false, features = ["alloc"] }

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
panic = "abort"

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
http-body-util = "0.1"
reqwest = { version = "0.12", default-features = false, features = ["json", "multipart", "rustls-tls"] }
